spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/spring_cloud_ordering_service/config-repo.git
          username: 754576021@qq.com
          password: bgkp108512
          basedir: /e/Users/bgkp/IdeaProjects/config/basedir #local repo
  rabbitmq:
    host: 192.168.1.21
    port: 5672
    username: guest
    password: guest
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: "*"